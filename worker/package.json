{
  "name": "sales-ai-manager-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate:local": "wrangler d1 migrations apply sales-ai-manager --local",
    "d1:migrate:remote": "wrangler d1 migrations apply sales-ai-manager --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  },
  "dependencies": {
    "postal-mime": "^2.7.5"
  }
}
